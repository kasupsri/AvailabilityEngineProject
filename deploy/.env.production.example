# Image version to run (default: latest)
IMAGE_VERSION=latest

# Public origin for API (used when building the web image). Use origin only - the app adds /api/ to paths.
# Example: https://availabilityengine.kasupsri.me (no trailing /api)
VITE_API_URL=https://availabilityengine.kasupsri.me

# Optional: if you run cloudflared on this host and use a token
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
