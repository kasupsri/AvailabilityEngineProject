# Image version to run (default: latest)
IMAGE_VERSION=latest

# Public API URL (used when building the web image; set before building if you build on this host)
# For production, set to the URL your Cloudflare Tunnel exposes, e.g. https://availability.yourdomain.com/api
VITE_API_URL=https://your-availability-domain.com/api

# Optional: if you run cloudflared on this host and use a token
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
